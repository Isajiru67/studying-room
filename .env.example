# このファイルをコピーして .env.local を作成し、値を入れてください（.env.local はgit管理外）。

# Supabase プロジェクトの URL / 公開用キー（ブラウザに公開されるキー）
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# サーバー専用の管理者キー（Settings > API > service_role）。
# RLSを無視できるため、絶対に NEXT_PUBLIC_ を付けない／コミットしない。
SUPABASE_SERVICE_ROLE_KEY=
